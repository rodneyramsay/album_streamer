pcm.!default {
	type plug
	slave.pcm "softvol"
}

ctl.!default {
	type hw
	card 0
}

pcm.softvol {
	type softvol
	slave {
		pcm "hw:0"
	}
	control {
		name "Amp"
		card 0
	}
	min_dB -50.0
	max_dB 0.0
	resolution 256
}
